name: 🚀 Build & Deploy Monitoring Stack

on:
  push:
    branches: [ main ]

env:
  ACR_NAME:       shiyomonitoring001
  IMAGE_NAME:     monitoring-app
  RESOURCE_GROUP: monitoring-rg
  APP_NAME:       monitoring-webapp

jobs:
  # ─────────────────────────────────────
  # JOB 1 — Build & Push Docker Image
  # ─────────────────────────────────────
  build:
    name: 🐳 Build & Push Image
    runs-on: ubuntu-latest
    steps:
      - name: ✅ Checkout code
        uses: actions/checkout@v4

      - name: ☁️ Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 🔐 Login to ACR
        run: az acr login --name ${{ env.ACR_NAME }}

      - name: 🐳 Build Docker image
        run: |
          docker build \
            -t ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:latest \
            -t ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }} \
            -f docker/Dockerfile ./app

      - name: 📤 Push image to ACR
        run: |
          docker push ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:latest
          docker push ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: ✅ Verify image in ACR
        run: az acr repository list --name ${{ env.ACR_NAME }} --output table

  # ─────────────────────────────────────
  # JOB 2 — Deploy to Azure App Service
  # ─────────────────────────────────────
  deploy:
    name: 🚀 Deploy to Azure App Service
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: ✅ Checkout code
        uses: actions/checkout@v4

      - name: ☁️ Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 🚀 Deploy to App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.APP_NAME }}
          images: ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: ✅ Print app URL
        run: |
          echo "✅ Deployed successfully!"
          echo "🌐 URL: https://${{ env.APP_NAME }}.azurewebsites.net/health"